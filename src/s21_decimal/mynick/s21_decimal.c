#include "s21_decimal.h"

/// |----------------------------------------------------|
/// | ~ ~ ~ ~ ~ ~ ~ ~ functions helpers ~ ~ ~ ~ ~ ~ ~ ~ ~|
/// |----------------------------------------------------|

void s21_set_scale(s21_decimal *value, int newExp) {
  value->exp = newExp;
}

// void set_bit() {
//   rResult.bits[i/32] |= (bit << (i%32));
// }

/// |----------------------------------------------------|
/// | - - - - - - - additional functions - - - - - - - - |
/// |----------------------------------------------------|

